/* exported getLastCharacter */

function getLastCharacter(string) {
  var lastCharacterIndex = string.length - 1;
  var lastCharacterResults = string.charAt(lastCharacterIndex);
  return lastCharacterResults;
}
